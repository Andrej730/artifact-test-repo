name: CCache Test

on:
  push:
    branches: [ master ]

jobs:
  ccache-test:
    runs-on: ubuntu-latest

    steps:
    - name: Install ccache
      run: |
        sudo apt-get update
        sudo apt-get install -y ccache

    - name: List ccache directory
      run: ls /usr/lib/ccache